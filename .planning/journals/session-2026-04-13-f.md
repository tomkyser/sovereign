# Session Journal — 2026-04-13-f

## Objective
Phase 2b-gaps-2 completion, then REPL coexist hardening: glob fix, safety hooks, binary patch for tool guidance, natural-language testing.

## Completed

### Phase 2b-gaps-2 (3/3 gaps — no code changes)
- **G15:** RESOLVED — already working. Bash tool shell snapshot shadows find→bfs 4.1 and grep→ugrep 7.5.0. F18 recorded.
- **G9-test:** Fetch prompt effectiveness verified with httpbin.org.
- **G11-test:** Persistence prompt effectiveness verified (var/state/const semantics).

### Glob Fix (Option B)
- Replaced find-based glob() with `rg --files --glob` — matches CC's native GlobTool.
- `**/*.ts` patterns now work. Results sorted by mtime via `--sort=modified`.
- Exit code 1 (no matches) caught gracefully.

### 4 Standalone Safety Hooks (no GSD dependency)
- `read-before-edit.cjs` — PreToolUse:Edit|Write advisory
- `commit-validate.cjs` — PreToolUse:Bash, blocks short commit messages
- `repl-precheck.cjs` — PreToolUse:REPL, blocks writes to .env/.git/lockfiles
- `repl-safety.cjs` — PostToolUse:REPL, auto-format + lint on modified files
- All deployed to ~/.claude/hooks/ and registered in settings.json.
- Hooks module for setup/launch pinned pre-M7.

### REPL Coexist Guidance (3 layers)
1. **Binary patch** — `writeReplToolGuidance()` injects into mk5() "Using your tools" section. Peer-level with "Use Read instead of cat." First attempt broke JS syntax (regex too greedy). Fixed: inject as new array element before `].filter`. 17/17 SOVEREIGN.
2. **Prompt override** — "Doing tasks (ambitious tasks)" extended with REPL batch guidance.
3. **REPL tool prompt** — "When NOT to use REPL" section added.

### Natural Language Testing
- Binary patch achieved goal: model now STARTS with REPL from natural language prompts.
- But: glob returned node_modules (--no-ignore), large files caused cascading errors, model fell back to bare tools on errors. Tracked as 2b-gaps-3.

## Findings
- **F18:** Bash tool shell snapshot shadows find/grep with embedded tools.
- Binary patch first attempt: regex matched across array elements, injected broken escaping into JS. Probe caught it (halt and catch fire). Fix: targeted the array closing pattern instead of the content span.

## Commits
- `c98dd58` Phase 2b-gaps-2 COMPLETE
- `af7f3a2` Glob fix: rg --files --glob
- `031218e` 4 standalone safety hooks
- `07850ad` REPL coexist guidance: prompt override + tool prompt
- `0b412e1` Binary patch: REPL guidance in Using your tools

## State at End
- **Build:** 160KB
- **Check:** 17/17 SOVEREIGN
- **Handlers:** 7/7 functional (glob rg-based, embedded search confirmed)
- **Hooks:** 4 standalone deployed + registered
- **Next:** Phase 2b-gaps-3 (G16-G23: glob defaults, model behavior, hooks module)
